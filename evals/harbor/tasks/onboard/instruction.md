A new contributor has just cloned the repository at `/app` and has never worked
on this project. Get their environment to a passing gate, then orient them from
project memory: what this package produces, what does not exist yet, and the
data caveats that would make a number wrong.
